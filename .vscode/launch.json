{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "attach",
            "name": "(lldb) Attach",
            "pid": "${command:pickMyProcess}"
        },
    ]
}